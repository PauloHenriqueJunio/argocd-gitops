# Checando a branch main!

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ivodocker-prod
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/PauloHenriqueJunio/ivodocker.git'
    path: prod
    targetRevision: main
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true