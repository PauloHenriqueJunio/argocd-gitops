apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ivodocker-prod-rollout
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "https://github.com/PauloHenriqueJunio/argocd-gitops.git"
    path: .
    targetRevision: main
    directory:
      include: "{rollout-prod.yaml,services-prod.yaml}"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
